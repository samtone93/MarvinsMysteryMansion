# inventory -> room
#def put(item):
    # inventory["objects"] = [i for i in inventory["objects"] if item not in i]
    # current_room["objects"].update(item)
    # edit inventory json
    # edit room json


# room -> inventory
#def take(item):
    # inventory["objects"].update(item)
    # current_room["objects]"] = [i for i in current_room["objects"] if item not in i]
    # inventory["objects."]
    # edit inventory
    # edit room
